# macOS
.DS_Store

# SwiftPM
.build/
.build-arm64/
.build-x86_64/
.swiftpm/
Package.resolved

# Bauergebnisse und Artefakte
build/
dist/
*.dmg
*.zip

# Nie einchecken: Signier- und Notarisierungsmaterial
*.p12
*.cer
*.certSigningRequest
AuthKey_*.p8
notarytool-*.json
SHA256SUMS

# Werkstatt der Bildschirmfotos. Ohne Pfad davor, damit der Eintrag greift,
# egal wo das Skript sie anlegt: ein falsch verankerter Eintrag hat sie beim
# ersten Mal durchgelassen, und nur der Waechter hat es gemerkt.
.shots/
/SyncTool-Beispiel/
